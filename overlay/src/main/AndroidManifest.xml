<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Foreground service + special-use declaration (API 34 requirement). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- READING_BATTERY_STATS is a normal permission; BatteryManager level is
         available without it, but the app requests it explicitly to gate the
         device.read_battery tool behind a real permission check. -->
    <uses-permission android:name="android.permission.BATTERY_STATS" />

    <application>
        <service
            android:name=".ShimejiOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Desktop mascot overlay companion app" />
        </service>
    </application>
</manifest>